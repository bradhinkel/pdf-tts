# XTTS-v2 experiment — install into a Python 3.11 venv (TTS requires Python <3.12)
# python3.11 -m venv .venv-xtts
# source .venv-xtts/bin/activate
# pip install -r requirements-xtts.txt

# PyTorch with CUDA (for GPU inference — remove +cu124 suffix for CPU-only)
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1+cu124
torchaudio==2.5.1+cu124

TTS==0.22.0
soundfile==0.12.1
numpy==1.26.4

# Also install the main app deps into this venv
fastapi==0.115.0
uvicorn[standard]==0.30.6
pdfplumber==0.11.4
httpx==0.27.2
python-dotenv==1.0.1
python-multipart==0.0.9
